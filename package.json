{
  "name": "kotak-neo-application",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:server": "NODE_ENV=development node api/index.js",
    "dev:client": "cd client && npm run dev",
    "build:client": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "express": "^4.19.0"
  }
}
